name: "equity_ops"
version: "1.0.0"
config-version: 2

profile: "equity_ops"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  equity_ops:
    staging:
      +materialized: view
    marts:
      +materialized: table
