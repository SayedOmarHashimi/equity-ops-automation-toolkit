pandas>=2.0
numpy>=1.24
yfinance>=0.2   # optional: real NVDA prices. Without it (or with --offline), prices are simulated.
streamlit>=1.30 # Stage 6 dashboard (dashboard.py); also required by Streamlit Community Cloud deploys
